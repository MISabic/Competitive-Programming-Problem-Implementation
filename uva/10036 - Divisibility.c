#include<stdio.h>

long long arr[1000000];
long long con(long long n)
{

}

int main()
{
    long long n;
    memset(arr,-1,sizeof(arr));
    while(scanf("%d",&n)==1){
        if()
    }
    return 0;
}
