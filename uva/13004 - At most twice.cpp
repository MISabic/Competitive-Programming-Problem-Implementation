#include<bits/stdc++.h>
using namespace std;

int main()
{
    char arr[30];
    while(scanf(" %s",arr)==1){
        int len=strlen(arr);
        for(int i=len[]; i<len; i++){
            arr
        }
    }
    return 0;
}
